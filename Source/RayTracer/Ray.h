#pragma once
#include <glm/glm.hpp>
struct ray_t
{
	ray_t() = default;
	ray_t(const glm::vec3& origin, const glm::vec3& direction) :
		< ? ? ? >,
		< ? ? ? >
	{}

		 GetPoint(float distance) const { <return point> }

	<3d origin>
		<3d direction>
};